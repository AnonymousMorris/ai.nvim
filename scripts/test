#!/bin/sh
set -eu

root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$root"

NVIM=${NVIM:-nvim}

for test in tests/*_e2e.lua; do
    printf '\n==> %s\n' "$test"
    "$NVIM" --headless -u NONE -l "$test"
done
